// static/js/main.js
console.log("We Guide Attendance System JS Loaded");

// You can add general JavaScript functions here.
// For example, functions to enhance UI, handle form submissions dynamically (if not using standard POST), etc.

// Example: Confirm before submitting a delete form (if not using modals for everything)
// document.querySelectorAll('.delete-confirm-button').forEach(button => {
//     button.addEventListener('click', function(event) {
//         if (!confirm('Are you sure you want to delete this item?')) {
//             event.preventDefault();
//         }
//     });
// });

// The camera logic is currently in templates/index.html for simplicity of access to Flask's url_for.
// If it grows complex, it could be moved here and URLs passed via data attributes.
